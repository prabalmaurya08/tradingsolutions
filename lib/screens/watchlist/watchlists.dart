import 'package:flutter/material.dart';

class Watchlist2 extends StatelessWidget {
  const Watchlist2({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Icon(Icons.document_scanner,size: 60,),
          const SizedBox(height: 20),
          const Text('Nothing to Show Here'),
          const SizedBox(height: 20), 
          const Text('Add from SearchBar to see here')
        ]

      ),
    );
  }
}


class Watchlist3 extends StatelessWidget {
  const Watchlist3({super.key});

  @override
  Widget build(BuildContext context) {
  return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Icon(Icons.document_scanner,size: 60,),
          const SizedBox(height: 20),
          const Text('Nothing to Show Here'),
          const SizedBox(height: 20), 
          const Text('Add from SearchBar to see here')
        ]

      ),
    );
  }
}
class Watchlist4 extends StatelessWidget {
  const Watchlist4({super.key});

  @override
  Widget build(BuildContext context) {
 return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Icon(Icons.document_scanner,size: 60,),
          const SizedBox(height: 20),
          const Text('Nothing to Show Here'),
          const SizedBox(height: 20), 
          const Text('Add from SearchBar to see here')
        ]

      ),
    );
  }
}
class Watchlist5 extends StatelessWidget {
  const Watchlist5({super.key});

  @override
  Widget build(BuildContext context) {
  return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Icon(Icons.document_scanner,size: 60,),
          const SizedBox(height: 20),
          const Text('Nothing to Show Here'),
          const SizedBox(height: 20), 
          const Text('Add from SearchBar to see here')
        ]

      ),
    );
  }
}